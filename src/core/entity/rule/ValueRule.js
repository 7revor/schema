import { Rule } from "./Rule";

export class MaxValueRule extends Rule {
  constructor(rule) {
    super(rule);
  }
}

export class MinValueRule extends Rule {
  constructor(rule) {
    super(rule);
  }
}